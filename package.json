{
  "name": "mega-tools",
  "version": "0.0.1",
  "description": "Mega Tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rimraf ./dist && npx tsc",
    "test": "npx tsc && node --test",
    "test-q": "node --test",
    "test-c": "rimraf ./dist && npx tsc && node --test"
  },
  "author": "GArysTrousers",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.6.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
  }
}
